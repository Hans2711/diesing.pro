// Trigger a direct private message via a minimal Laravel endpoint
window.sendWsDirect = (data, receiverId) => {
    window.Echo.connector.pusher.send_event('client-direct-message', {
        senderId: sessionId,
        receiverId,
        data: btoa(data) // base64 encode for safe transport
    }, `private-user.${receiverId}`);
};
